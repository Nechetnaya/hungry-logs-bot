# 🍎 HungryLogs — Техническое задание и план реализации

## 🎯 Цель проекта
Создать Telegram-бота с ИИ-агентом, который:
- Определяет цели пользователя и рассчитывает БЖУ/калории;
- Принимает записи о еде в свободной форме;
- Считает калории и БЖУ, обновляет их в Google Sheets;
- Показывает дневной прогресс и графики;
- Подбирает рацион под цель и имеющиеся продукты.

## ⚙️ Технологический стек

| Компонент | Технология |
|-----------|------------|
| Telegram-бот | aiogram |
| ИИ-агент (обработка текста) | OpenAI API (GPT-4/5) |
| Таблицы данных | Google Sheets (через gspread + сервисный аккаунт) |
| Среда исполнения | Python 3.10+ |

## 🧩 Функциональные блоки

### 1. Регистрация и цели
- Бот запрашивает: возраст, пол, рост, вес, активность, цель (сброс/набор);
- Считает норму калорий и БЖУ (формула Mifflin–St Jeor);
- Сохраняет профиль пользователя на лист **users** в Google Sheets.

**Структура листа `users`**:

| user_id | age | sex | height | weight | activity | goal | target_cal | p_goal | f_goal | c_goal |

---

### 2. Приём пищи
- Пользователь пишет в свободной форме (например: “овсянка с молоком и бананом”);
- Бот:
  - Передаёт текст в GPT → получает JSON с продуктами и БЖУ;
  - Сохраняет запись в лист **meals**;
  - Считает суточный итог и отвечает пользователю.

**Структура листа `meals`**:

| user_id | date | meal_text | protein | fat | carbs | calories |

---

### 3. Прогресс
- Бот считает суммарное потребление за день (`SUMIF(user_id, date)` в Python);
- Отвечает:  
  `"Сегодня: 95 г белка из 120, 1800 из 2200 ккал."`

---

### 4. Подбор рациона
- Пользователь пишет: `"Составь рацион на 1800 ккал, у меня есть рыба и творог"`;
- GPT формирует рацион с блюдами, порциями и итоговыми калориями;
- Бот возвращает текстовое меню.

---

### 5. Графики
- Пользователь пишет: `"Покажи график за неделю"`;
- Бот выгружает данные из Google Sheets, строит график калорийности, отправляет PNG.

---

# 📂 Структура проекта nutrition_bot

```text
nutrition_bot/
│
├── app/                             # Основной код бота
│   ├── main.py                      # Точка входа (aiogram)
│   │
│   ├── handlers/                    # Обработчики команд и сообщений
│   │   ├── registration.py          # Сбор данных пользователя
│   │   ├── meals.py                 # Ввод приёмов пищи
│   │   ├── progress.py              # Подсчёт суммарной статистики
│   │   ├── recommendation.py        # Подбор рациона
│   │   └── charts.py                # Построение графиков
│   │
│   ├── services/                    # Вспомогательные сервисы
│   │   ├── gsheet_client.py         # Работа с Google Sheets (gspread)
│   │   ├── openai_client.py         # Запросы к GPT
│   │   └── nutrition_utils.py       # Расчёт БЖУ и калорий
│   │
│   └── config.py                    # Токены, ключи API и настройки
│
├── tests/                           # Тесты для бота и сервисов
│   ├── test_registration.py
│   ├── test_meals.py
│   └── test_nutrition_utils.py
│
├── requirements.txt                 # Список зависимостей Python
├── README.md                        # Описание проекта
└── .env                             # Файл с переменными окружения (токены, ключи)
```
---

## 🗓️ План реализации

### Этап 1 — Базовая структура
- Создать Google Sheets и добавить 2 листа: `users`, `meals`
- Настроить сервисный аккаунт и подключение через gspread
- Настроить Telegram-бота (aiogram)
- Реализовать сохранение и загрузку данных из Sheets

### Этап 2 — Расчёт целей
- Диалог по сбору параметров
- Формула Mifflin–St Jeor
- Запись профиля пользователя

### Этап 3 — Приём пищи
- Ввод еды в свободной форме
- Обработка через OpenAI (парсинг текста → JSON)
- Запись в таблицу и вывод текущего прогресса

# ⚙️ Техническое задание: модуль трекинга веса и интеллектуального анализа прогресса

## 📘 Цель
Добавить в Telegram-бот систему отслеживания веса и анализа прогресса пользователя (на основе динамики веса и рациона).  
ИИ-агент должен формировать персональные рекомендации: оценивать динамику, давать корректировки по калорийности и БЖУ, а также при необходимости — корректировать цель.

---

## 📁 Структура проекта
```
app/
│
├── bot.py                  # основной бот и диспетчер
├── registration.py         # регистрация пользователя
├── meals.py                # трекинг приёмов пищи
├── weight.py ✅             # новый модуль веса
│
├── utils/
│   ├── csv_client.py       # CSVClient (расширенный)
│   ├── ai_analysis.py ✅    # модуль общения с ИИ
│
└── data/
    ├── users.csv
    ├── meals.csv
    └── weights.csv ✅
```

---

## ⚙️ 1. Файл `weights.csv`
**Пример:**
```
user_id	date	weight
12345678	2025-10-21	68.3
```

- Хранится в `data/weights.csv`.
- Создаётся автоматически при инициализации `CSVClient`.

**Формат:**
- `user_id` — int  
- `date` — ISO (YYYY-MM-DD)  
- `weight` — float  

---

## ⚙️ 2. Расширение `CSVClient`

Добавить методы:
```python
def add_weight(self, user_id: int, weight: float, weight_date: str = None)
def get_weights(self, user_id: int) -> list[dict]
def get_last_weight(self, user_id: int) -> Optional[float]
def get_weight_history(self, user_id: int, days: int = 30) -> list[dict]
```

**Описание:**
- `add_weight()` — добавляет запись (с `date.today()` по умолчанию)  
- `get_weights()` — возвращает все записи по `user_id`  
- `get_last_weight()` — возвращает последнее измерение по дате  
- `get_weight_history()` — фильтрует по периоду, используется для анализа  

---

## 📲 3. Команды и меню

**Главное меню (ReplyKeyboardMarkup)**

| Команда | Описание |
|----------|-----------|
| 📊 Ввести вес | команда `/weight` |
| 🧠 Анализ прогресса | команда `/analyze` |
| 🎯 Изменить цель | команда `/goal` |

> Команды доступны только зарегистрированным пользователям.

---

## ⚖️ 4. FSM для веса и цели
```python
class WeightTracking(StatesGroup):
    waiting_weight = State()

class GoalSetting(StatesGroup):
    waiting_goal = State()
    waiting_custom_cal = State()
```

---

## ⚖️ 5. Команда `/weight`

**Сценарий:**

1. Пользователь выбирает “📊 Ввести вес” в меню.  
2. Бот:  
   ```
   Введите текущий вес в кг (например, 68.5):
   ```
3. Пользователь вводит вес.  
4. Вес сохраняется в `weights.csv`.  
5. Бот отвечает:  
   ```
   ✅ Вес 68.5 кг сохранён! Анализирую прогресс…
   ```
6. Вызывается `analyze_progress(user_id)`.

---

## 🧠 6. Модуль `ai_analysis.py`

**Функция:**
```python
async def analyze_progress(user_id: int) -> str:
    ...
```

**Сбор данных:**
- Профиль из `users.csv`  
  (возраст, пол, рост, цель, норма калорий и БЖУ)  
- История веса за последние 30 дней (`weights.csv`)  
- Средние калории и БЖУ по приёмам пищи за 30 дней (`meals.csv`)

---

### 📋 Пример сводки, передаваемой в ИИ
```
Пользователь: Женщина, 35 лет, рост 168 см
Цель: похудение
Текущий вес: 67.5 кг
Вес 30 дней назад: 69.8 кг
Изменение: -2.3 кг

Средние показатели за 30 дней:
- Калории: 1760 (норма 1800)
- Белки: 90 г (норма 95 г)
- Жиры: 70 г (норма 65 г)
- Углеводы: 200 г (норма 210 г)

Замечания: тренировки 3 раза в неделю, активность средняя.
```

---

### 🧠 Пример запроса к ИИ
```
Проанализируй динамику веса и питания пользователя.
Дай рекомендации: стоит ли корректировать цель, норму калорий или БЖУ.
```

---

### 🧾 Пример ответа ИИ
```
📉 За 4 недели вес снизился на 2.3 кг — стабильная и здоровая динамика.
Рекомендация:
- Оставить калорийность около 1750 ккал.
- Немного увеличить белок до 100 г/день.
- Цель корректировать не нужно.
```

---

## 🔁 7. Еженедельное напоминание о взвешивании

**Функционал:**
Каждое воскресенье в 10:00 бот шлёт:  
```
Напоминание: пора обновить вес ⚖️
Напиши /weight, чтобы внести новое значение.
```

**Реализация:**  
Через `apscheduler` или асинхронный цикл при запуске.

---

## 🎯 8. Команда `/goal`

Показывает текущую цель и нормы:

```
Текущая цель: похудение  
Калории: 1800  
БЖУ: 100 / 60 / 180
```

**Кнопки:**
- 🏃 Похудение  
- ⚖️ Поддержание  
- 💪 Набор массы  
- ✍️ Задать вручную  

При выборе бот обновляет `users.csv`.

---

## 📈 9. Команда `/analyze`

Запускает `analyze_progress(user_id)` вручную.  
ИИ формирует и отправляет отчёт, даже если новый вес не введён.

---

## 💬 10. Пример сценария

```
📊 Ввести вес
→ "Введите текущий вес (например, 67.4)"
→ "✅ Вес 67.4 кг сохранён! Анализирую..."
```

🤖 **ИИ-отчёт:**
```
Вес стабильно снижается (-2.4 кг за 5 недель).
Среднее потребление — 1750 ккал, белков чуть ниже нормы.
Рекомендую добавить 10 г белка и оставить калории без изменений.
```

---

## 🧱 11. Основные модули

| Модуль | Назначение |
|--------|-------------|
| `weight.py` | FSM логика: ввод веса, сохранение, вызов анализа |
| `ai_analysis.py` | Сбор данных, подготовка сводки, запрос в ИИ |
| `csv_client.py` | Расширен методами работы с весом |
| `bot.py` | Меню, маршрутизация команд, планировщик напоминаний |

---

## ✅ 12. Проверка

- При `/start` меню добавляется пользователю.  
- Команда `/weight` работает только для зарегистрированных.  
- После сохранения веса вызывается ИИ.  
- ИИ получает полную сводку (вес + питание + цель).  
- Есть возможность ручного вызова анализа `/analyze`.  
- Еженедельное напоминание приходит автоматически.  
- Всё сохраняется в `data/*.csv`.
- Добавить сохранение калорий и бжу без ИИ
